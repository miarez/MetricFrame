// utils/config.js

// All heatmap colors are centralized here.
// Swap these to change the palette.
export const HeatmapConfig = {
  // Low values
  minColor: "#c0392b", // red-ish
  // High values
  maxColor: "#27ae60", // green-ish

  // Used for null/NaN or missing values
  nullColor: "#444444",

  // Text color on top of heatmap cells
  textColor: "#ffffff",
};
